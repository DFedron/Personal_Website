let form = document.querySelector('form');
form.addEventListener('submit', handleSubmit);
  
function handleSubmit(event) {
    alert("This form hasn't implemented, thus it won't save anything. Pleach contact through email huidong@pdx.edu");
}